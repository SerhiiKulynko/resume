<div>
    {{#with main}}
        <main>
            
                    {{>title data=title big=true}}
                    
            <section>
                
                <ul>
                    {{#each colleges}}

                        <li>
                            {{name}}
                            {{#unless isEnd}}(ще в процесі){{/unless}}
                        </li>
                        
                    {{/each}}
                </ul>

            </section>
            {{>title data="My certificates" big=false}}

            <ul>
                {{#each certificates}}
                    <li>
                        {{name}} ({{id}})
                    </li>
                {{/each}}
            </ul>

            <section>

            </section>
        </main>     
    {{/with}}
</div>